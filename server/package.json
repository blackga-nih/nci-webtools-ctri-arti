{
  "name": "server",
  "version": "1.0.0",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "env -u HOSTNAME node --env-file .env --import=./polyfills.js server.js",
    "start:dev": "env -u HOSTNAME node --env-file .env --import=./polyfills.js --watch-path=. server.js",
    "test:setup": "npx playwright install chromium",
    "test": "node --env-file test.env --test --experimental-test-coverage --test-reporter=tap && npm run test:integration",
    "test:unit": "node --env-file test.env --test --experimental-test-coverage --test-reporter=tap",
    "test:integration": "node --env-file test.env integration.js",
    "cert": "openssl req -x509 -newkey rsa:2048 -nodes -subj '/C=US/ST=State/L=City/O=Organization/CN=localhost' -keyout key.tmp -out cert.tmp && cat key.tmp cert.tmp > key.pem && rm key.tmp cert.tmp"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1003.0",
    "@aws-sdk/client-textract": "^3.1003.0",
    "@aws-sdk/client-translate": "^3.1003.0",
    "cms": "*",
    "database": "*",
    "drizzle-orm": "^0.45.1",
    "express": "^5.2.1",
    "express-session": "^1.19.0",
    "gateway": "*",
    "handlebars": "^4.7.8",
    "mammoth": "^1.11.0",
    "node-cron": "^4.2.1",
    "node-forge": "^1.3.3",
    "nodemailer": "^8.0.1",
    "oidc-provider": "^9.6.1",
    "openid-client": "^6.8.2",
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^5.5.207",
    "shared": "*"
  },
  "devDependencies": {
    "playwright": "^1.58.2",
    "supertest": "^7.2.2"
  }
}
